[package]
name = "dods"
version = "0.1.0"
edition = "2024"

[dependencies]
rocket = "0.5.1"
serde = {version = "1.0.219", features=["derive"]}
serde_json = "1.0.140"
tokio = {version = "1", features=["full"]}
